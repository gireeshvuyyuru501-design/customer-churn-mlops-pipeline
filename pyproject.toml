[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "churn"
version = "0.1.0"
description = "AWS-native MLOps pipeline for customer churn prediction"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
